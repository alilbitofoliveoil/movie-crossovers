/*COPY THIS ENTIRE LINE TO BROWSER CONSOLE WHILE NAVIGATED TO AN IMDB LIST*/const f = async l => {console.log(`Fetching URI: ${l}`);return new DOMParser().parseFromString(await (await fetch(l)).text(), 'text/html')};const lk = await f(prompt('What is your other IMDB link?'));const p1 = d => [...d.querySelectorAll('.lister-item-header > a,.titleColumn > a')].map(e => e.textContent);const p2 = d => d.querySelector('.list-name, .header').textContent.trim();const r = p1(lk);URL.createObjectURL(new Blob([`Possible Movie Crossovers: '${p2(document)}' and '${p2(lk)}' from IMDb\n\n`, ...p1(document).flatMap(o => r.map(t => `${o} + ${t}\n`))], {type: 'text/plain'}))
